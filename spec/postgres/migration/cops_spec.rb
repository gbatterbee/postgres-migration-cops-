RSpec.describe Postgres::Migration::Cops do
  it "has a version number" do
    expect(Postgres::Migration::Cops::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
